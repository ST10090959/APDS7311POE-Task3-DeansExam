version: 2

jobs:
  sonar_scan:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: SonarQube Scan
          command: |
            npm install -g sonarqube-scanner
            sonar-scanner \
              -Dsonar.projectKey=ST10090959_APDS7311POE-Task3-DeansExam \
              -Dsonar.organization=st10090959 \
              -Dsonar.host.url="https://sonarcloud.io" \
              -Dsonar.login=04388e58943c2a21d6e637e1065129f17f3b5fdb \
workflows:
  version: 2
  sonar_workflow:
    jobs:
      - sonar_scan



